experiments:
  - name: tracer-microbenchmarks
    steps:
      - name: benchmarks
        cpus: 24-47
        run: shell
        script: |
          /platform/steps/run-benchmarks.py 24-47 2 "span tracer sampling_rule_matches set_http_meta core_api otel_span rate_limiter telemetry_add_metric http_propagation_extract http_propagation_inject"
